services:
  spectre-linux:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spectre-linux
    hostname: spectre
    privileged: true
    stdin_open: true
    tty: true
    environment:
      - TERM=xterm-256color
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    volumes:
      # Mount persistent data only (spectre files copied during build)
      #- ../../spectre:/opt/spectre:ro
      # Persistent data
      - spectre-data:/var/lib/spectre
      # Logs accessible from host
      - ./logs:/var/log/spectre
    ports:
      - "22:22"        # SSH
      - "80:80"        # HTTP
      - "6379:6379"    # Redis
      - "21:21"        # FTP
    networks:
      - spectre-net
    cap_add:
      - NET_ADMIN
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined

  # Optional: Vulnerable targets for testing (from lab)
  vuln-redis:
    image: redis:6
    container_name: spectre-test-redis
    command: redis-server --protected-mode no
    networks:
      - spectre-net
    profiles:
      - lab

  vuln-mongo:
    image: mongo:4.4
    container_name: spectre-test-mongo
    environment:
      - MONGO_INITDB_DATABASE=test
    networks:
      - spectre-net
    profiles:
      - lab

  vuln-mysql:
    image: mysql:5.7
    container_name: spectre-test-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=password123
      - MYSQL_DATABASE=testdb
    networks:
      - spectre-net
    profiles:
      - lab

volumes:
  spectre-data:
  spectre-logs:

networks:
  spectre-net:
    driver: bridge
